HUGO ROVELLI




Pour la modelisation du  design1:

	il devrait avoir un local assignement entre (DNO et DNOS ) et (DN et DNOS) nous ne l'avons pas ajouter etant donnée que green modofie notre code.

Pour la modelisation du  design2:

	il ya une relation d'agregation entre DNO et Local2DNOS (il ya un tableau de DNO dans Local2DNOS) que green ne permet pas d'afficher,aussi le getInstance du server est appeler dans la methode main,il devrait donc y avoir une relation d'instantiation que green n'affiche pas. 

Pour la modelisation du design3:
	meme probleme pour le diagramme precedent (il y'a une relation d'instanciation entre le main et l'un des serveur et il ya un tableau de DNO dans Local2DNOS que green n'affiche pas
